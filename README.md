This repo is still a Work in Progress! 

# Usage Instructions:

- To install the required packages: `uv pip install -r requirements.txt` or `pip install -r requirements.txt`.
- Then, the main notebook is `text_gerrychain_demo.ipynb`. There will eventually be an extra `src` directory with some data cleaning code.